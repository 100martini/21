# Checkers Game Server

This service runs the real-time multiplayer logic for the Checkers game.
It uses Node.js and Socket.io to manage game rooms, player connections, and game state.

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```

2. Start the server:
   ```bash
   npm run start
   ```
